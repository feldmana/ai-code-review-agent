diff --git a/src/main/java/com/agentic/codereview/agent/ReviewAgent.java b/src/main/java/com/agentic/codereview/agent/ReviewAgent.java
index cbb0171..711764f 100644
--- a/src/main/java/com/agentic/codereview/agent/ReviewAgent.java
+++ b/src/main/java/com/agentic/codereview/agent/ReviewAgent.java
@@ -1,8 +1,8 @@
 package com.agentic.codereview.agent;
 
 import com.agentic.codereview.llm.OllamaClient;
-import com.agentic.codereview.rag.RagService;
 import com.agentic.codereview.prompt.ReviewPromptTemplate;
+import com.agentic.codereview.rag.service.RagService;
 import com.google.gson.Gson;
 
 import java.nio.file.Files;
@@ -37,11 +37,11 @@ public class ReviewAgent {
 
                 String code = Files.readString(file);
 
-                // ✅ RAG IS NOW USED
+                // 🔥 RAG
                 List<String> rules = ragService.getRelevantRules(code);
-                String rulesText = String.join("\n\n", rules);
+                String rulesText = formatRules(rules);
 
-                // ✅ USE YOUR PROMPT TEMPLATE
+                // 🔥 Prompt
                 String prompt = String.format(
                         ReviewPromptTemplate.REVIEW_PROMPT,
                         file.getFileName(),
@@ -58,4 +58,20 @@ public class ReviewAgent {
             throw new RuntimeException("Review failed", e);
         }
     }
+
+    /**
+     * Formats rules in a structured way for LLM clarity
+     */
+    private String formatRules(List<String> rules) {
+
+        StringBuilder sb = new StringBuilder();
+
+        for (int i = 0; i < rules.size(); i++) {
+            sb.append("RULE SET ").append(i + 1).append(":\n")
+                    .append(rules.get(i))
+                    .append("\n\n");
+        }
+
+        return sb.toString();
+    }
 }
\ No newline at end of file
diff --git a/src/main/java/com/agentic/codereview/config/AppConfig.java b/src/main/java/com/agentic/codereview/config/AppConfig.java
index 7c88d93..c0e3593 100644
--- a/src/main/java/com/agentic/codereview/config/AppConfig.java
+++ b/src/main/java/com/agentic/codereview/config/AppConfig.java
@@ -4,18 +4,26 @@ import java.io.FileInputStream;
 import java.io.IOException;
 import java.util.Properties;
 
-/**
- * Application configuration loaded from properties file or environment variables
- */
 public class AppConfig {
-    private static final String CONFIG_FILE = "codereview.properties";
 
-    // Ollama Configuration
-    public static final String OLLAMA_HOST = "http://localhost";
-    public static final int OLLAMA_PORT = 11434;
-    public static final String OLLAMA_MODEL = "llama3"; // Can be overridden
+    private static final String CONFIG_FILE = "codereview.properties";
+    private static AppConfig instance;
 
-    // Email Configuration
+    // =========================
+    // RAG
+    // =========================
+    private final RagMode ragMode;
+
+    // =========================
+    // OLLAMA (new)
+    // =========================
+    private final String ollamaHost;
+    private final int ollamaPort;
+    private final String ollamaModel;
+
+    // =========================
+    // EMAIL (old)
+    // =========================
     private boolean emailEnabled;
     private String emailTo;
     private String smtpHost;
@@ -24,17 +32,16 @@ public class AppConfig {
     private String smtpPassword;
     private boolean smtpTlsEnabled;
 
-    // Review Configuration
+    // =========================
+    // REVIEW (old)
+    // =========================
     private int maxRetries = 3;
     private boolean enableParallelProcessing = true;
     private int threadPoolSize = 4;
 
-    private static AppConfig instance;
-
-    public AppConfig() {
-        loadConfiguration();
-    }
-
+    // =========================
+    // Singleton
+    // =========================
     public static AppConfig getInstance() {
         if (instance == null) {
             instance = new AppConfig();
@@ -42,48 +49,115 @@ public class AppConfig {
         return instance;
     }
 
-    private void loadConfiguration() {
-        Properties props = new Properties();
+    private AppConfig() {
+        Properties props = loadProps();
 
-        // Try to load from file
-        try (FileInputStream fis = new FileInputStream(CONFIG_FILE)) {
-            props.load(fis);
-        } catch (IOException e) {
-            System.out.println("Configuration file not found, using environment variables and defaults");
-        }
+        // ===== RAG =====
+        this.ragMode = loadRagMode();
+
+        // ===== OLLAMA =====
+        this.ollamaHost = getProperty(props, "ollama.host",
+                System.getProperty("ollama.host"), "http://127.0.0.1");
 
-        // Email Configuration
+        this.ollamaPort = Integer.parseInt(
+                getProperty(props, "ollama.port",
+                        System.getProperty("ollama.port"), "11434")
+        );
+
+        this.ollamaModel = getProperty(props, "ollama.model",
+                System.getProperty("ollama.model"), "llama3");
+
+        // ===== EMAIL (OLD) =====
         this.emailEnabled = Boolean.parseBoolean(
-                getProperty(props, "EMAIL_ENABLED", System.getenv("EMAIL_ENABLED"), "false")
+                getProperty(props, "EMAIL_ENABLED",
+                        System.getenv("EMAIL_ENABLED"), "false")
         );
-        this.emailTo = getProperty(props, "EMAIL_TO", System.getenv("EMAIL_TO"), "");
-        this.smtpHost = getProperty(props, "SMTP_HOST", System.getenv("SMTP_HOST"), "smtp.gmail.com");
+
+        this.emailTo = getProperty(props, "EMAIL_TO",
+                System.getenv("EMAIL_TO"), "");
+
+        this.smtpHost = getProperty(props, "SMTP_HOST",
+                System.getenv("SMTP_HOST"), "smtp.gmail.com");
+
         this.smtpPort = Integer.parseInt(
-                getProperty(props, "SMTP_PORT", System.getenv("SMTP_PORT"), "587")
+                getProperty(props, "SMTP_PORT",
+                        System.getenv("SMTP_PORT"), "587")
         );
-        this.smtpUsername = getProperty(props, "SMTP_USERNAME", System.getenv("SMTP_USERNAME"), "");
-        this.smtpPassword = getProperty(props, "SMTP_PASSWORD", System.getenv("SMTP_PASSWORD"), "");
+
+        this.smtpUsername = getProperty(props, "SMTP_USERNAME",
+                System.getenv("SMTP_USERNAME"), "");
+
+        this.smtpPassword = getProperty(props, "SMTP_PASSWORD",
+                System.getenv("SMTP_PASSWORD"), "");
+
         this.smtpTlsEnabled = Boolean.parseBoolean(
-                getProperty(props, "SMTP_TLS_ENABLED", System.getenv("SMTP_TLS_ENABLED"), "true")
+                getProperty(props, "SMTP_TLS_ENABLED",
+                        System.getenv("SMTP_TLS_ENABLED"), "true")
         );
 
-        // Review Configuration
+        // ===== REVIEW (OLD) =====
         this.maxRetries = Integer.parseInt(
-                getProperty(props, "MAX_RETRIES", System.getenv("MAX_RETRIES"), "3")
+                getProperty(props, "MAX_RETRIES",
+                        System.getenv("MAX_RETRIES"), "3")
         );
+
         this.threadPoolSize = Integer.parseInt(
-                getProperty(props, "THREAD_POOL_SIZE", System.getenv("THREAD_POOL_SIZE"), "4")
+                getProperty(props, "THREAD_POOL_SIZE",
+                        System.getenv("THREAD_POOL_SIZE"), "4")
         );
     }
 
-    private String getProperty(Properties props, String propName, String envValue, String defaultValue) {
-        if (envValue != null && !envValue.isEmpty()) {
-            return envValue;
+    // =========================
+    // Helpers
+    // =========================
+    private Properties loadProps() {
+        Properties props = new Properties();
+
+        try (FileInputStream fis = new FileInputStream(CONFIG_FILE)) {
+            props.load(fis);
+        } catch (IOException e) {
+            System.out.println("Config file not found, using env/system defaults");
         }
-        return props.getProperty(propName, defaultValue);
+
+        return props;
+    }
+
+    private String getProperty(Properties props, String key, String env, String def) {
+        if (env != null && !env.isEmpty()) return env;
+        return props.getProperty(key, def);
+    }
+
+    private RagMode loadRagMode() {
+        String mode = System.getProperty("rag.mode", "KEYWORD");
+        return RagMode.valueOf(mode.toUpperCase());
+    }
+
+    // =========================
+    // Getters (NEW)
+    // =========================
+    public RagMode getRagMode() {
+        return ragMode;
+    }
+
+    public String getOllamaHost() {
+        return ollamaHost;
+    }
+
+    public int getOllamaPort() {
+        return ollamaPort;
     }
 
-    // Getters
+    public String getOllamaModel() {
+        return ollamaModel;
+    }
+
+    public String getOllamaUrl() {
+        return ollamaHost + ":" + ollamaPort;
+    }
+
+    // =========================
+    // Getters (OLD EMAIL)
+    // =========================
     public boolean isEmailEnabled() { return emailEnabled; }
     public String getEmailTo() { return emailTo; }
     public String getSmtpHost() { return smtpHost; }
@@ -91,23 +165,11 @@ public class AppConfig {
     public String getSmtpUsername() { return smtpUsername; }
     public String getSmtpPassword() { return smtpPassword; }
     public boolean isSmtpTlsEnabled() { return smtpTlsEnabled; }
+
+    // =========================
+    // Getters (OLD REVIEW)
+    // =========================
     public int getMaxRetries() { return maxRetries; }
     public boolean isParallelProcessingEnabled() { return enableParallelProcessing; }
     public int getThreadPoolSize() { return threadPoolSize; }
-
-    @Override
-    public String toString() {
-        return "AppConfig{" +
-                "emailEnabled=" + emailEnabled +
-                ", smtpHost='" + smtpHost + '\'' +
-                ", smtpPort=" + smtpPort +
-                ", maxRetries=" + maxRetries +
-                ", threadPoolSize=" + threadPoolSize +
-                '}';
-    }
-
-    public String getOllamaUrl() {
-        return OLLAMA_HOST + ":" + OLLAMA_PORT;
-    }
-}
-
+}
\ No newline at end of file
diff --git a/src/main/java/com/agentic/codereview/config/RagMode.java b/src/main/java/com/agentic/codereview/config/RagMode.java
new file mode 100644
index 0000000..7c8c8e0
--- /dev/null
+++ b/src/main/java/com/agentic/codereview/config/RagMode.java
@@ -0,0 +1,6 @@
+package com.agentic.codereview.config;
+
+public enum RagMode {
+    KEYWORD,
+    VECTOR
+}
\ No newline at end of file
diff --git a/src/main/java/com/agentic/codereview/factory/AgentFactory.java b/src/main/java/com/agentic/codereview/factory/AgentFactory.java
index 5ce410b..c32dc26 100644
--- a/src/main/java/com/agentic/codereview/factory/AgentFactory.java
+++ b/src/main/java/com/agentic/codereview/factory/AgentFactory.java
@@ -3,19 +3,33 @@ package com.agentic.codereview.factory;
 import com.agentic.codereview.agent.ReviewAgent;
 import com.agentic.codereview.config.AppConfig;
 import com.agentic.codereview.llm.OllamaClient;
-import com.agentic.codereview.rag.RagService;
+import com.agentic.codereview.rag.service.KeywordRagService;
+import com.agentic.codereview.rag.service.RagService;
+// import com.agentic.codereview.rag.service.VectorRagService;
 
 public class AgentFactory {
 
     public static ReviewAgent create(AppConfig config) {
 
         OllamaClient ollamaClient = new OllamaClient(
-                AppConfig.OLLAMA_HOST,
-                AppConfig.OLLAMA_PORT,
-                AppConfig.OLLAMA_MODEL
+                config.getOllamaHost(),
+                config.getOllamaPort(),
+                config.getOllamaModel()
         );
 
-        RagService ragService = new RagService();
+        RagService ragService;
+
+        switch (config.getRagMode()) {
+
+            case KEYWORD -> ragService = new KeywordRagService();
+
+            // later you add this
+            // case VECTOR -> ragService = new VectorRagService();
+
+            default -> throw new IllegalStateException(
+                    "Unknown RAG mode: " + config.getRagMode()
+            );
+        }
 
         return new ReviewAgent(
                 ollamaClient,
diff --git a/src/main/java/com/agentic/codereview/model/RuleType.java b/src/main/java/com/agentic/codereview/model/RuleType.java
new file mode 100644
index 0000000..d7b304c
--- /dev/null
+++ b/src/main/java/com/agentic/codereview/model/RuleType.java
@@ -0,0 +1,7 @@
+package com.agentic.codereview.config;
+
+public enum RuleType {
+    ARCHITECTURE,
+    EXAMPLES,
+    NAMING
+}
\ No newline at end of file
diff --git a/src/main/java/com/agentic/codereview/prompt/ReviewPromptTemplate.java b/src/main/java/com/agentic/codereview/prompt/ReviewPromptTemplate.java
index aff93aa..75f93b4 100644
--- a/src/main/java/com/agentic/codereview/prompt/ReviewPromptTemplate.java
+++ b/src/main/java/com/agentic/codereview/prompt/ReviewPromptTemplate.java
@@ -21,21 +21,30 @@ STRICT OUTPUT RULES:
 - Never truncate output
 - Never include logs, timestamps, or comments
 - Ensure all arrays and objects are properly closed
+- Do NOT combine multiple values in any field
 
-FORMAT:
+FORMAT (STRICT SCHEMA):
 {
   "issues": [
     {
-      "type": "naming|architecture|bug|style|performance",
-      "severity": "LOW|MEDIUM|HIGH",
+      "type": "naming",
+      "severity": "LOW",
       "message": "string",
       "suggestion": "string"
     }
   ],
   "suggestions": ["string"],
-  "severity": "LOW|MEDIUM|HIGH"
+  "severity": "LOW"
 }
 
+FIELD RULES:
+- type MUST be exactly ONE of:
+  naming | architecture | bug | style | performance
+- severity MUST be exactly ONE of:
+  LOW | MEDIUM | HIGH
+- NEVER combine values (NO pipes, NO lists, NO concatenation)
+- Each issue must have a single type only
+
 FILE:
 %s
 
diff --git a/src/main/java/com/agentic/codereview/rag/factory/RagFactory.java b/src/main/java/com/agentic/codereview/rag/factory/RagFactory.java
new file mode 100644
index 0000000..35e05df
--- /dev/null
+++ b/src/main/java/com/agentic/codereview/rag/factory/RagFactory.java
@@ -0,0 +1,24 @@
+package com.agentic.codereview.rag.factory;
+
+import com.agentic.codereview.config.AppConfig;
+import com.agentic.codereview.rag.service.KeywordRagService;
+import com.agentic.codereview.rag.service.RagService;
+import com.agentic.codereview.rag.service.VectorRagService;
+
+public class RagFactory {
+
+    public static RagService create(AppConfig config) {
+
+        switch (config.getRagMode()) {
+
+            case KEYWORD:
+                return new KeywordRagService();
+
+            case VECTOR:
+                return new VectorRagService();
+
+            default:
+                throw new IllegalStateException("Unknown mode");
+        }
+    }
+}
\ No newline at end of file
diff --git a/src/main/java/com/agentic/codereview/rag/RagService.java b/src/main/java/com/agentic/codereview/rag/service/KeywordRagService.java
similarity index 73%
rename from src/main/java/com/agentic/codereview/rag/RagService.java
rename to src/main/java/com/agentic/codereview/rag/service/KeywordRagService.java
index 413006f..ca6cddb 100644
--- a/src/main/java/com/agentic/codereview/rag/RagService.java
+++ b/src/main/java/com/agentic/codereview/rag/service/KeywordRagService.java
@@ -1,4 +1,4 @@
-package com.agentic.codereview.rag;
+package com.agentic.codereview.rag.service;
 
 import java.io.IOException;
 import java.nio.file.Files;
@@ -6,7 +6,7 @@ import java.nio.file.Path;
 import java.util.ArrayList;
 import java.util.List;
 
-public class RagService {
+public class KeywordRagService implements RagService {
 
     private static final String RULES_PATH = "rag-docs/rules";
 
@@ -20,15 +20,15 @@ public class RagService {
         List<String> rules = new ArrayList<>();
 
         try {
-            // Load all rule files
             List<Path> files = Files.list(Path.of(RULES_PATH))
                     .filter(p -> p.toString().endsWith(".md"))
                     .toList();
 
             for (Path file : files) {
+                String fileName = file.getFileName().toString().toLowerCase();
                 String content = Files.readString(file);
 
-                if (isRelevant(file.getFileName().toString(), code)) {
+                if (isRelevant(fileName, code)) {
                     rules.add(content);
                 }
             }
@@ -41,24 +41,22 @@ public class RagService {
     }
 
     /**
-     * VERY SIMPLE relevance logic (we'll upgrade later to embeddings)
+     * Simple keyword-based relevance (V1 baseline)
      */
     private boolean isRelevant(String fileName, String code) {
 
-        fileName = fileName.toLowerCase();
-
         if (fileName.contains("naming")) {
             return code.contains("class") || code.contains("void");
         }
 
         if (fileName.contains("architecture")) {
-            return code.contains("Controller") ||
-                    code.contains("Service") ||
-                    code.contains("Repository");
+            return code.contains("Controller")
+                    || code.contains("Service")
+                    || code.contains("Repository");
         }
 
         if (fileName.contains("quality")) {
-            return true; // always relevant
+            return true;
         }
 
         if (fileName.contains("bad-examples")) {
diff --git a/src/main/java/com/agentic/codereview/rag/service/RagService.java b/src/main/java/com/agentic/codereview/rag/service/RagService.java
new file mode 100644
index 0000000..6f899db
--- /dev/null
+++ b/src/main/java/com/agentic/codereview/rag/service/RagService.java
@@ -0,0 +1,7 @@
+package com.agentic.codereview.rag.service;
+
+import java.util.List;
+
+public interface RagService {
+    List<String> getRelevantRules(String code);
+}
\ No newline at end of file
diff --git a/src/main/java/com/agentic/codereview/rag/service/VectorRagService.java b/src/main/java/com/agentic/codereview/rag/service/VectorRagService.java
new file mode 100644
index 0000000..f3ec380
--- /dev/null
+++ b/src/main/java/com/agentic/codereview/rag/service/VectorRagService.java
@@ -0,0 +1,10 @@
+package com.agentic.codereview.rag.service;
+
+import java.util.List;
+
+public class VectorRagService implements RagService {
+    @Override
+    public List<String> getRelevantRules(String code) {
+        return List.of();
+    }
+}
